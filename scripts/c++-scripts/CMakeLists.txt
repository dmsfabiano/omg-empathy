project( FaceDetector )
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
find_package( OpenCV REQUIRED )
add_executable( FaceDetector detectFaceAndLandmarks.cpp )
target_link_libraries( FaceDetector ${OpenCV_LIBS} -lstdc++fs )
